apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication
metadata:
  name: inventory-postgres-auth
  namespace: ecommerce
spec:
  secretTargetRef:
    - parameter: connection
      name: inventory-postgres-secret
      key: connection
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: inventory-service-outbox-lag
  namespace: ecommerce
spec:
  scaleTargetRef:
    name: inventory-service
  pollingInterval: 30
  cooldownPeriod: 300
  minReplicaCount: 1
  maxReplicaCount: 4
  fallback:
    failureThreshold: 3
    replicas: 1
  triggers:
    - type: postgresql
      metadata:
        query: "SELECT COUNT(*) FROM outbox_event WHERE published_at IS NULL"
        targetQueryValue: "100"
      authenticationRef:
        name: inventory-postgres-auth
