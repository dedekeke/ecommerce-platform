apiVersion: apps/v1
kind: Deployment
metadata:
  name: shell-app
  namespace: ecommerce
  labels:
    app: shell-app
    tier: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shell-app
  template:
    metadata:
      labels:
        app: shell-app
        tier: frontend
    spec:
      containers:
        - name: shell-app
          image: ecommerce/shell-app:1.0.0
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: shell-app
  namespace: ecommerce
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      name: http
  selector:
    app: shell-app
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: shell-app-pdb
  namespace: ecommerce
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: shell-app
