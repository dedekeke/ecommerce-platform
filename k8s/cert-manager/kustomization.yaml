apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply after cert-manager is installed. ACME_EMAIL and BASE_DOMAIN must be exported, then:
#   for f in cluster-issuer.yaml certificate.yaml; do envsubst < $f; done | kubectl apply -f -

resources:
  - cluster-issuer.yaml
  - certificate.yaml
