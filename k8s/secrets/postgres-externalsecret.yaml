apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: postgres-secret
  namespace: ecommerce
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: postgres-secret
    creationPolicy: Owner
  data:
    - secretKey: username
      remoteRef:
        key: kv/data/postgres
        property: username
    - secretKey: password
      remoteRef:
        key: kv/data/postgres
        property: password
    - secretKey: connection
      remoteRef:
        key: kv/data/postgres
        property: connection
