# Per-service ExternalSecret (least-privilege). Materialises Secret `product-service-secret`
# from this service's dedicated Vault path plus the shared datastore paths it uses.
# The Vault role `eso-product-service` (see helm/vault/policies) grants read on exactly these paths.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: product-service-secret
  namespace: ecommerce
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-product-service
    kind: SecretStore
  target:
    name: product-service-secret
    creationPolicy: Owner
  data:
    - secretKey: APP_ENCRYPTION_KEY
      remoteRef: {key: kv/data/services/product-service, property: encryption_key}
    - secretKey: SPRING_DATASOURCE_USERNAME
      remoteRef: {key: kv/data/mysql, property: username}
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef: {key: kv/data/mysql, property: password}
    - secretKey: AUTH0_CLIENT_ID
      remoteRef: {key: kv/data/auth0, property: client_id}
    - secretKey: AUTH0_CLIENT_SECRET
      remoteRef: {key: kv/data/auth0, property: client_secret}
