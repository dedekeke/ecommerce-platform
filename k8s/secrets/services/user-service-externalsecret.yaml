# Per-service ExternalSecret (least-privilege). Materialises Secret `user-service-secret`
# from this service's dedicated Vault path plus the shared datastore paths it uses.
# The Vault role `eso-user-service` (see helm/vault/policies) grants read on exactly these paths.
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: user-service-secret
  namespace: ecommerce
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-user-service
    kind: SecretStore
  target:
    name: user-service-secret
    creationPolicy: Owner
  data:
    - secretKey: APP_ENCRYPTION_KEY
      remoteRef: {key: kv/data/services/user-service, property: encryption_key}
    - secretKey: SPRING_DATASOURCE_USERNAME
      remoteRef: {key: kv/data/postgres, property: username}
    - secretKey: SPRING_DATASOURCE_PASSWORD
      remoteRef: {key: kv/data/postgres, property: password}
    - secretKey: AUTH0_CLIENT_ID
      remoteRef: {key: kv/data/auth0, property: client_id}
    - secretKey: AUTH0_CLIENT_SECRET
      remoteRef: {key: kv/data/auth0, property: client_secret}
