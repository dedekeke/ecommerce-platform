/**
 * Test-only utilities for MicroFrontendLoader.
 *
 * Import this file only in test code — it is excluded from production bundles
 * because it is never referenced by non-test source files.
 */
export { __resetMFELazyComponents } from './lazyRegistry'
